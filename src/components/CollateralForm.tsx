'use client'

// Import the modular form
import { CollateralFormModular } from './collateral-form'

// Re-export with the same interface for backward compatibility
export default CollateralFormModular
